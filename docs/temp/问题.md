P1: final_prompt 丢失了 relevant columns。

prompt_payload.py (line 164) 把 SqlGenerationContext.schema_context.tables 转成 prompt table 时固定写了 "columns": []，没有把 knowledge_pipeline.py (line 571) 里已经生成的 schema_context.columns 分组挂到对应表下。实际输出里每张表都有 Columns:，但下面是空的。这样 LLM 看不到 emp_id / dept_nm / emp_stat_cd / dept_id 这些关键字段，和“最终提示词能看到选中的表、字段、关系和值绑定”的目标不一致。测试也漏了这个点，建议补断言 final prompt 包含这些字段。




P2: schema linking 会无条件选中 HR 表字段，即使没有候选和 hints。

knowledge_pipeline.py (line 472) 到 knowledge_pipeline.py (line 487) 不看 question.metric_hints / dimension_hints，也不看 retrieval 是否命中，直接加入 hr_emp_base、hr_dept_dim、emp_id、dept_nm。我用空 candidates + 空 hints 验证了一下，仍然会选出 HR 两张表和 join。当前 mock 阶段不一定马上炸，但这个函数已经是中间层核心规则，后续一换问题处理结果就会产生错误上下文。建议改成只有存在对应 hint 或候选证据时才补充。
